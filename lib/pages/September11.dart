import 'package:flutter/material.dart';

class September11 extends StatelessWidget {
  const September11({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("I love September 11")
    );
  }
}