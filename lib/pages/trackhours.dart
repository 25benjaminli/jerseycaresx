import 'package:flutter/material.dart';

class TrackHoursPage extends StatelessWidget {
  const TrackHoursPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("No hours for u")
    );
  }
}